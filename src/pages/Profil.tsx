import { Box } from "@mui/material";

const Profil = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        minHeight: "100vh",
        marginTop: "40px",
      }}
    >
      test
    </Box>
  );
};

export default Profil;
